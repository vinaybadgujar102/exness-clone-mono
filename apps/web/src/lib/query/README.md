# Query Layer

React Query is the source of truth for all server-backed data.

- Keep query keys centralized in `query-keys.ts`.
- Keep client defaults in `query-client.ts`.
- Use hooks in this directory for screen-level data composition.
